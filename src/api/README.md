API client notes:

- `apiClient` reads base URL from `import.meta.env.VITE_API_BASE_URL`.
- Add additional typed modules for other controllers (iOS, Monitoring, Version) following `android.ts` pattern.
